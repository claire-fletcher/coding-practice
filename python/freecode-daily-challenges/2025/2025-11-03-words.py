def count_words(sentence):
    # split by space and then count len of the returned 
    words = sentence.split(" ")
    return len(words)

# Todays was very simple. Find the number of words in a sentence where all words split by space.